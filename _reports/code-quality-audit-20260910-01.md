# Wmark 测试后代码质量审计

## 结论

PASS，96.5 / 100，high 风险门槛 90。之所以按 high 审查，是应用可能用于证件等私人图片；应用本身没有账号系统和远程图片服务。机器计算结果见同名 JSON。

范围：全部一方界面、IPC、图像核心、文件输出、配置、构建脚本和对应测试。排除第三方源码、生成二进制和用户资料。此报告评估源码与已执行本地证据；平台 CI 和发行最终状态由完整测试报告记录，不把审计分数当作无缺陷证明。

## 评分

| 维度 | 分数 | 门槛 | 核心证据 |
| --- | --- | --- | --- |
| 需求忠实度 | 15/15 | 14 | R01–R10 基线、实现与用例矩阵 |
| 逻辑与异常 | 20/20 | 18 | alpha、位置、元数据、取消、失败重试断言 |
| 安全与隐私 | 14/15 | 14 | 本地路径授权、CSP、输入校验、生产 npm 审计 |
| 数据与并发 | 15/15 | 14 | 原子输出、不覆盖、四并发文件输出测试 |
| 性能与资源 | 9/10 | 9 | 24MP 实测、100 小图批量、大小边界 |
| 可维护性 | 8.5/10 | 8 | 独立核心与 IPC、可替换接口；大组件和冗余依赖扣分 |
| 测试证据 | 10/10 | 9 | 54 项本地测试、Mac 实际导出像素核对 |
| 交付就绪 | 5/5 | 4 | 当前包安装、启动、ad-hoc 校验、DMG 完整性、报告 |

## 已验证事实

- npm test：20 个测试通过；cargo test --workspace：29 核心 + 5 桌面验证测试通过。
- TypeScript、生产前端构建、cargo clippy -D warnings 与 cargo fmt 通过。
- Mac `.app` 和 DMG 成功生成；最终安装包主程序 hash 与构建包相等。
- codesign 完整性和 hdiutil verify 通过；otool 仅列系统动态库，不依赖 Homebrew lcms2。
- 最终已安装版导出的 1400×1050 PNG 与同参数核心渲染逐像素严格一致。
- npm 生产依赖审计没有报告漏洞；这不等于完整供应链安全保证。

## 发现与剩余维护项

无未关闭 critical/high。以下为 4 个 low，不影响已覆盖功能：

| ID | 位置 | 事实与影响 | 后续 |
| --- | --- | --- | --- |
| CQ-001 | Cargo.lock | 未穷尽 Rust advisory 数据库；npm 审计不能代替 Rust 审计 | 正式稳定发布前加入 RustSec 定期扫描 |
| CQ-002 | src-tauri/src/lib.rs:14 | 导入授权路径集合保留至退出；长时间反复导入移除会累积路径字符串，不保留解码图像 | 增加路径释放与会话策略 |
| CQ-003 | src/App.tsx:112 | 工作台 JSX 组件较大，可维护性成本上升 | 下一次扩展界面时拆分设置/任务/列表组件 |
| CQ-004 | src-tauri/Cargo.toml:15 | 存在 base64/image 冗余直接依赖 | 清理 manifest 并重新构建验证 |

已经修复的 alpha 舍入、签名资源和缺失文件前置阻断问题见测试报告；对应回归测试未删除或弱化。

## 推断与未覆盖

- 两端使用相同内置字体和核心算法，预期水印规则一致；实际 CI 验证范围见测试报告，不能据此推断所有硬件输出一模一样。
- 未完成 Windows 人工视觉/安装交互验收、所有 DPI、所有 ICC/字形组合、磁盘损坏/断电实验、长时间会话压力测试。
- 无平台开发者签名或公证；只能按公开说明发布预览版本。

## 复审入口

`npm run check`、`npm run build`、`cargo fmt --all -- --check`、`CI=true npm run tauri -- build --bundles app,dmg`、合成图桌面导出和 `verify_output`，加对应平台 Actions。源码修复后重跑受影响检查与全量回归。
