# 测试报告：界面标题改为「悟鸣水印工作台」

- 日期：2026-09-25
- 测试环境：macOS（arm64，Apple Silicon）
- 源码版本：工作区未提交修改，改动文件为 `src/App.tsx`、`src-tauri/tauri.conf.json`

## 改动范围

1. `src/App.tsx:442` 顶栏文案 `水印工作台` → `悟鸣水印工作台`
2. `src-tauri/tauri.conf.json:15` 窗口标题 `Wmark · 水印工作台` → `Wmark · 悟鸣水印工作台`

原型文件 `prototype/index.html` 为历史设计稿，不在运行产物内，本次不修改。

## 测试用例与结果

| 用例 | 预期 | 实际结果 |
| --- | --- | --- |
| 前端单元/组件测试 `npm test` | 全部通过 | 26/26 通过（model.test.ts 5、App.test.tsx 21） |
| 全量检查 `npm run check`（typecheck + vitest + cargo test + clippy） | 全部通过 | 通过：vitest 26/26；cargo test 33 passed、0 failed；clippy `-D warnings` 无告警 |
| 构建 `npm run desktop:build` | 产物为本次源码、arm64 | 通过：`target/release/bundle/macos/Wmark.app` + `Wmark_0.1.0_aarch64.dmg`；二进制内含新标题 `Wmark · 悟鸣水印工作台`；前端 bundle `dist/assets/index-UnWIaqXe.js` 含新文案 |
| 安装后启动 | 新进程来自 `/Applications/Wmark.app` | 通过：新进程 PID 58555，路径 `/Applications/Wmark.app/Contents/MacOS/wmark` |
| 界面验收 | 顶栏显示「悟鸣水印工作台」，窗口标题同步更新 | 通过：窗口标题 `Wmark · 悟鸣水印工作台`、顶栏文案 `悟鸣水印工作台` 均实际渲染；窗口四边、圆角完整，新文案无截断遮挡，其余布局与设置项无变化；用户模板与上次设置完整保留 |

## 签名基线（旧安装版）

- 路径：`/Applications/Wmark.app`
- Bundle ID：`io.github.chujianyun.wmark`
- 签名：ad-hoc（`Signature=adhoc`，`TeamIdentifier=not set`），项目固定配置 `signingIdentity: "-"`，本次沿用
- 旧 DR：`cdhash H"4ff09fb2d6679a03990cecd811f147447bc19c7a"`
- `codesign --verify --deep --strict`：通过
- 说明：ad-hoc 签名的 DR 绑定 CDHash，随代码变化属预期；本应用不使用 TCC 受控权限，无授权连续性风险

## 安装与启动验证

- 旧进程：PID 54554（`/Applications/Wmark.app/Contents/MacOS/wmark`），经 AppleScript 请求正常退出，`pgrep` 确认无残留
- 安装：旧包备份至 `/tmp/Wmark.app.bak-20260925`，新包完整复制至 `/Applications/Wmark.app`；用户数据（模板、上次设置）未触碰，启动后确认保留
- 新产物签名：ad-hoc、arm64、`codesign --verify --deep --strict` 安装前后均通过；Bundle ID 不变
- 启动：`open /Applications/Wmark.app`，新进程 PID 58555 持续运行
- 界面验收条件：窗口 1320×900（默认尺寸），浅色外观；截图证据 `.spec-to-ship/desktop-v1/screenshots/20260925-header-rename.jpg`，含完整窗口四边与圆角
- 交互抽验：AX 树确认窗口标题、顶栏文案、导出按钮、模板下拉等控件齐全且状态正确（无图片时导出按钮禁用符合预期）

## Git 交付

不适用：项目 `AGENTS.md` 未约定提交/推送要求，本次未获推送授权；工作区尚有其他任务的未提交修改，本次两个文件的改动保留在工作区不混入提交。

## 发布结果（v0.2.0）

- 提交：`7ae8974`（本机字体功能）、`7e0c578`（标题改名 + 版本 0.2.0），已推送 `main` 及标签 `v0.2.0`
- 发布前本地检查：`npm run check` 通过（vitest 26/26、cargo test 33/33、clippy 无告警），`cargo metadata --locked` 锁文件一致，`node scripts/check-version.mjs` 版本一致 0.2.0
- CI 流水线 [run 36027225961](https://github.com/chujianyun/wmark/actions/runs/36027225961)：macOS arm64 DMG、macOS x86_64 DMG、Windows x64 NSIS 三个构建任务及 release 任务全部 success
- Release：https://github.com/chujianyun/wmark/releases/tag/v0.2.0 （prerelease，沿用既有预览版策略）
- 产物：`Wmark_0.2.0_aarch64.dmg`（17.2 MB）、`Wmark_0.2.0_x64.dmg`（17.5 MB）、`Wmark_0.2.0_x64-setup.exe`（13.6 MB）、`SHA256SUMS.txt`
