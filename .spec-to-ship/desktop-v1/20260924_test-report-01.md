# Wmark 测试报告 20260924-01：本机字体选择功能交付

## 改动范围

工作区未提交改动（基于 `f955707`），为文字水印新增本机字体选择功能：

- `crates/wmark-core/src/model.rs`：`Watermark` 新增 `font_family` 字段（默认 `Noto Sans CJK SC`，serde 默认值兼容旧配置），并校验字体名非空、≤256 字符、无控制字符。
- `crates/wmark-core/src/imaging.rs`：新增 `font_families()` 枚举本机字体；渲染与导出使用所选字体，不可用字体回退内置思源黑体且不解释 markup。
- `src-tauri/src/lib.rs`：新增 `list_fonts` 命令。
- `src/api.ts`、`src/App.tsx`：字体下拉框改为本机字体列表（内置字体显示为「思源黑体（内置）」，不可用字体标注回退），新增字体读取状态提示；加粗独立为开关。
- `src/model.ts`：`Watermark` / `defaultSpec` 增加 `fontFamily`。
- 测试：`crates/wmark-core/tests/acceptance.rs`、`tests/App.test.tsx` 新增字体相关用例。
- `README.md`、`Cargo.lock`、`crates/wmark-core/Cargo.toml` 同步更新。

## 测试环境与源码版本

- macOS（arm64），Rust stable，Node + npm，Tauri 2。
- 源码：`f955707` + 上述未提交修改（本次交付包含未提交改动）。

## 自动化测试（已执行）

| 命令 | 结果 |
| --- | --- |
| `npm run typecheck`（tsc -b） | 通过 |
| `npm test`（vitest run） | 2 个文件 26 个用例全部通过（含「selects a local font for preview and export independently of bold」） |
| `cargo test --workspace` | wmark_lib 5 通过；wmark-core acceptance 33 通过（含 `font_family_survives_settings_roundtrip`、`old_settings_default_to_bundled_font`、`macos_local_font_changes_pixels_and_matches_export`、`unavailable_font_falls_back_without_interpreting_markup`）；0 失败 |
| `cargo clippy --workspace --all-targets -- -D warnings` | 通过，无警告 |

完整日志：`artifacts/check-20260924.log`。

## 待执行

- [x] 全部完成，详见下方「交付验证结果」。

## 交付验证结果

### 构建与产物

- `npm run desktop:build`（tauri build）成功，日志 `artifacts/build-20260924.log`。
- 产物：`target/release/bundle/macos/Wmark.app`（arm64）及 `target/release/bundle/dmg/Wmark_0.1.0_aarch64.dmg`，均来自本次未提交源码。

### 签名与身份

- 项目固定签名配置为 `signingIdentity: "-"`（ad-hoc + Hardened Runtime），已安装旧版同为 ad-hoc（TeamIdentifier 未设置），身份类型一致，未切换身份。ad-hoc 的 DR 绑定 cdhash，随构建变化属正常现象；本机无 TCC 特殊权限依赖（文件均经用户选择/拖入），无授权继承问题。
- 安装前：`codesign --verify --deep --strict --all-architectures` 通过，bundle id `io.github.chujianyun.wmark` 与旧版一致。
- 安装后复查：同样校验通过，Identifier/Signature=adhoc 与候选产物一致。

### 旧进程、安装、启动

- 旧进程 PID 53748（/Applications/Wmark.app/Contents/MacOS/wmark）经 AppleScript 请求正常退出，2 秒内退出，无 SIGTERM/SIGKILL，无残留。
- 旧包备份至 `artifacts/Wmark-before-fonts-20260924.app`，新包经 ditto 完整替换 `/Applications/Wmark.app`，未触碰用户配置（`~/Library/Application Support/io.github.chujianyun.wmark/preferences.json` 保留）。
- `open "/Applications/Wmark.app"` 启动，PID 53833 运行路径确认为新安装包；期间用户实际操作后退出（无崩溃报告，日志为正常退出）；再次启动 PID 54554，持续运行。

### 界面与功能验收（已安装新版本，默认窗口 1320×900）

- 字体下拉框：显示「思源黑体（内置）」；点击展开本机字体菜单，实际列出 Academy Engraved LET、Al Bayan、American Typewriter、Andale Mono、Apple 系列、Arial 系列等本机字体，当前项带勾选。截图 `screenshots/20260924-font-menu-local-fonts.jpg`。
- 字体状态提示「安装新字体后，重启应用即可使用」正常显示；「加粗」为独立开关，与字体选择解耦。
- 样式门禁：窗口顶部标题栏、左侧列表、中央预览、右侧设置面板完整；页面滚动后「导出图片」按钮与底部「保留原始尺寸 · 不覆盖原文件」、版本信息完整可见可操作。截图 `screenshots/20260924-main-window-top.jpg`（顶部）、`screenshots/20260924-scrolled-export-button-visible.jpg`（滚动后底部）。圆角、背景无异常色块。
- 导入链路实测：导入 landscape.png（1400×1050）成功，提示「导入完成: 1 张成功」，预览渲染正常。
- 核心渲染/导出由 cargo 验收测试覆盖：`macos_local_font_changes_pixels_and_matches_export`、`unavailable_font_falls_back_without_interpreting_markup`、`font_family_survives_settings_roundtrip`、`old_settings_default_to_bundled_font` 均通过。

### 并发操作说明

验证期间用户正在同时使用应用（应用模板、导入图片、打开导出对话框、退出应用）。观察到的设置变化（颜色 #5E30EB、大小 6%）为用户自行应用其已保存模板「AI 培训」的结果，`preferences.json` 保持用户选择未做回退。图片列表为会话内状态，重启后清空属正常行为。

### Git

本次任务仅要求安装启动，无提交/推送授权，工作区改动保持未提交状态。
